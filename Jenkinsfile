pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                sh 'cd'
                sh 'cd /var/lib/jenkins/workspace'
                sh 'sudo rm -rf *'
               }
            }
        }
    }
}
